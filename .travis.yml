language: python

python:
  - "3.7"

install:
  - pip install git+ssh://git@github.com/facebookexternal/botorch.git
  - pip install -q -e .[dev,mysql,notebook]

script:
  - black --check --diff .
  - flake8
  - pytest -ra
